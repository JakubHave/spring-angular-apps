package com.jakub.media.store.model;


// NOTE: RoleVoter which is part of the default configuration,
// will only respond to authorities which begin with the prefix "ROLE_"

public enum RoleEnum {
    ROLE_USER,
    ROLE_ADMIN
}
